import SignIn from "@/app/pages/authentication/login/SignIn";

export const metadata = {
  title: "Soundfolio | Sign In",
  description:
    "Sign in to your Soundfolio account to access your dashboard, bookings, and profile.",
};

const SignInPage = () => {
  return <SignIn />;
};

export default SignInPage;
